package ar.edu.unlp.objetos.uno.DEMO;
public class CalculadoraDeDistancia {
     
    //uso el constructor por defecto (vacio)
    
    /* Asuma que existe una clase CalculadoraDeDistancia, cuyas instancias entienden el mensaje #distanciaEntre 
        que recibe dos direcciones y retorna la distancia en Km entre ellas. Por ahora trabaje con una implementación
        propia para pruebas que siempre retorna 100 (sin importar las direcciones recibidas). 
    */ 
    public double distanciaEntre (String direccion1, String direccion2)
    {
        return 100;
    }
}
